export const PAGE_PATHS = {
  home: '/',
  dashboard: '/dashboard',
  login: '/login',
  authCallback: '/auth/callback',
};

export const HEADERLESS_PATHS = [PAGE_PATHS.login, PAGE_PATHS.authCallback];
